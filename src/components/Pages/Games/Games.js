import { Box, Typography } from "@mui/material";
import Navbar from "../../common/Navbar";

const Games = () => {
  return (
    <Box>
      <Navbar/>
      <Typography>
        Hola, mis amigo.
      </Typography>
    </Box>
  );
};

export default Games;
