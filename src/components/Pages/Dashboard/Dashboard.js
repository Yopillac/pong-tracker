import { Box, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Box>
      <Typography>
        Welcome, my friend.
      </Typography>
    </Box>
  );
};

export default Dashboard;
